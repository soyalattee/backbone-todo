image: node:12

stages:
  - deploy

pages:
  stage: deploy
  script:
    - rm -rf public
    - cp -rf * .public
    - echo "cp public successful"
    - ls public

  artifacts:
    paths:
      - public
  only:
    - master
  tags:
    - docker
